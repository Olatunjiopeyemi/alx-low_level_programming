#include<stdio.h>
#include<stdlib.h>
/**
 * main - entry point
 * print all lowercase letters
 * Return: 0
 */

	int main(void)
{
	for (ch = 'a'; ch <= 'z'; ch++)
	putchar(ch);
	putchar('\n');
return (0);
}

