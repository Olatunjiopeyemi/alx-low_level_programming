#include<stdio.h>
#include<stdlib.h>
/**
 * main - entry point
 * print all lowercase letters
 * Return: 0
 */

	int main(void)
{
	char lower;
	for (lower = 'a'; lower <= 'z'; lower++);
{
	putchar(lower);
}
putchar('\n');
return (0);
}

