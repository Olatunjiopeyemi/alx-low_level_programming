#include "main.h"

/**
 * mul - Checks for digits (0 through 9)
 * @b: int to be checked
 * @b: argument
 * Return: 0
 * @m: multiplication
 */
int mul(int a, int b)
{
	int m = a * b
		_putchar('0' + m);
}
