#include "main.h"

/**
 * _abs - body
 *
 *@x: argument
 * a function that prints the sign of a number
 *
 * Return: Absolute value
 *
 */

int _abs(int x)
{
	if (x >= 0)
	{
		return (x);
	}
	else
	{
		return (-x);
	}
}
