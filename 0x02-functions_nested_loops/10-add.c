#include "main.h"

/**
 * add - body
 * a function that adds two integers and returns the result.
 * r: addition
 * @a: argument
 * @b: argument
 * Return:r result
 */

int add(int a, int b)
{
	int r = a + b;

	return (r);
}
